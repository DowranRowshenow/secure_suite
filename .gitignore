# Melos internal tool folder (contains scripts and symlinks)
.melos_tool/

# Dart Workspace shared config (Generated by 'dart pub get' in root)
.dart_tool/

# Editor
.idea/

# If you use overrides for local testing
pubspec_overrides.yaml

# THE BIG DEBATE: pubspec.lock
# In a monorepo, you usually COMMIT the root pubspec.lock 
# but IGNORE package-level locks.
packages/**/pubspec.lock